/**
 * Visual manifest: entity type -> visual (CONVENTIONS.md / SETUP.md #4).
 * Upgrading asset tiers (shapes -> packs -> custom art) edits ONLY this file.
 */
import type { VisualManifest } from "@gf/framework/shell";

export const manifest: VisualManifest = {
  orb: { kind: "circle", r: 8, color: 0x4fc3f7 },
};
